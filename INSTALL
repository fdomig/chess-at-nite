Linux/MacOS Users

You need to have a g++ compiler installed.

If you don't have a previous version, try doing this:

$ git clone git://github.com/fdomig/chess-at-nite.git
$ cd chess-at-nite
$ make
$ cd bin
$ ./chess-at-nite
